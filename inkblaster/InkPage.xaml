<Page
    x:Class="inkblaster.InkPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:inkblaster"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Button Style="{StaticResource NavigationBackButtonNormalStyle}" Canvas.ZIndex="1"
                IsEnabled="{x:Bind Frame.CanGoBack, Mode=OneWay}" VerticalAlignment="Top" Click="returnToMain"/>
        <StackPanel x:Name="menu" Canvas.ZIndex="1" Visibility="Collapsed">
            <StackPanel Orientation="Horizontal">
                <Button Margin="0,0,0,0" Click="closeMenu"> <SymbolIcon Symbol="Back"/> </Button>
                <Button Margin="4,0,0,0" Click="extendCanvas"> <SymbolIcon Symbol="Add"/> </Button>
                <Button Margin="4,0,0,0" Click="saveFileButton"> <SymbolIcon Symbol="Save"/> </Button>
            </StackPanel>
            <InkToolbar TargetInkCanvas="{x:Bind inkCanvas}"/>
        </StackPanel>
        <ScrollViewer>
            <InkCanvas x:Name="inkCanvas" Height="2500"/>
        </ScrollViewer>
    </Grid>
</Page>
